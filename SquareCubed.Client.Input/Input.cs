using OpenTK;

namespace SquareCubed.Client.Input
{
    public class Input
    {
	    public Vector2 GetAxis()
	    {
			// TODO: Change so it actually does things
		    return new Vector2(1, 0);
	    }
    }
}
